<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.DeviceDefault">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:colorAccent">?android:attr/colorAccent</item>
        <item name="android:statusBarColor">?android:attr/colorAccent</item>
    </style>
    <style name="AppThemeDevice" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="TestTheme" parent="AppTheme" />
    <style name="MainTheme" parent="AppTheme" />

    <style name="AppBaseTheme.Light" parent="android:Theme.DeviceDefault.Light" />

    <style name="AppTheme.Light" parent="AppBaseTheme.Light" />

</resources>
